{
  "name": "squeeze-demos",
  "version": "1.0.0",
  "description": "Interactive demos for Squeeze dimensionality reduction library",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "generate": "python scripts/generate_embeddings.py"
  },
  "dependencies": {
    "regl-scatterplot": "^1.9.0"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}
